# Novel-to-Script Backend 依赖
# 安装方式: pip install -r requirements.txt

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# 核心（复用 scripts/ 模块）
pyyaml>=6.0
pydantic>=2.0

# 网络请求（提供商测试）
httpx>=0.27.0

# 环境变量
python-dotenv>=1.0.0

# AI 提供商（可选，AI 模式需要）
anthropic>=0.30.0
openai>=1.0.0
